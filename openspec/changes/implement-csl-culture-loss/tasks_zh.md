# CSL文化损失实施任务清单

## 第一阶段：核心CSL函数实现

### 1.1 实现CSL损失函数
- [x] 在`train_joint_lora_moe.py`中创建`compute_csl_culture_loss()`函数
- [x] 实现L_culture_router计算，使用成对余弦相似度
- [x] 实现L_culture_share计算，用于文化无关的共享专家损失
- [x] 实现L_culture_sr计算，用于共享-路由解耦损失
- [x] 添加适当的张量维度处理和批次大小验证
- [x] 包含余弦相似度计算的数值稳定性检查
- [x] 为边缘情况（空批次、零向量）添加全面的错误处理

### 1.2 专家输出提取
- [x] 验证JointLoRAMoEModel输出格式提供所需的专家输出
- [x] 实现路由专家输出（er_i向量）的提取
- [x] 实现共享专家输出（es_i向量）的提取
- [x] 实现路由权重（wr_i向量）的提取
- [x] 为专家输出张量形状和类型添加验证
- [x] 处理共享专家或路由输出不可用的情况

### 1.3 高效相似性计算
- [x] 为批处理实现向量化余弦相似度
- [x] 优化成对相似性计算的内存效率
- [x] 为大数据集添加批次大小考虑
- [x] 实现增量计算以避免内存溢出
- [x] 为所有相似性计算添加适当的梯度流保持

## 第二阶段：集成和配置

### 2.1 训练流水线集成
- [x] 修改`train_epoch_joint()`中的训练循环以检测`USE_CULTURE_LOSS=csl`
- [x] 在适当时用CSL替换现有文化损失
- [x] 保持与现有损失模式（`ori`、`new`、`kl`）的向后兼容性
- [x] 添加CSL损失组件日志记录和监控
- [x] 确保适当的损失权重和梯度累积

### 2.2 配置更新
- [x] 更新`run_joint_lora_moe_training.sh`默认`USE_CULTURE_LOSS`为`csl`
- [x] 在脚本注释中添加CSL特定参数文档
- [x] 更新配置验证以识别`csl`选项
- [x] 添加CSL损失组件权重配置选项
- [x] 更新训练配置JSON以包含CSL参数

### 2.3 模型输出验证
- [x] 验证JointLoRAMoEModel提供所需的专家输出格式
- [x] 为CSL要求添加模型输出验证
- [x] 增强模型前向传播以确保专家输出可用
- [x] 当专家输出缺失时添加回退行为
- [x] 使用不同模型配置（共享/无共享、门控/无门控）测试CSL

## 第三阶段：文档和验证

### 3.1 架构文档更新
- [x] 更新`culturemoe.md`以反映联合训练模型架构
- [x] 记录CSL损失函数数学公式
- [x] 描述三个CSL组件及其目标
- [x] 添加联合训练流水线描述，包含LoRA+MoE集成
- [x] 包含CSL参数指导和使用示例
- [x] 记录CSL功能的模型输出要求

### 3.2 损失函数文档
- [x] 记录L_culture_router设计和实现
- [x] 记录L_culture_share文化无关目标
- [x] 记录L_culture_sr解耦机制
- [x] 为所有三个组件添加数学公式
- [x] 包含计算复杂度分析
- [x] 记录CSL与现有文化损失的比较

### 3.3 全面测试
- [ ] 使用不同批次大小（1、2、4、8、16）测试CSL
- [ ] 验证CSL在不同文化标签分布下的行为
- [ ] 测试CSL内存使用和计算开销
- [ ] 验证与现有训练配置的向后兼容性
- [ ] 使用不同模型架构（LLaMA、Qwen）测试CSL
- [ ] 验证CSL梯度流和优化稳定性

## 第四阶段：质量保证和优化

### 4.1 性能验证
- [ ] 基准测试CSL计算开销与现有文化损失的对比
- [ ] 测量CSL实现的内存使用影响
- [ ] 分析CSL相似性计算的优化机会
- [ ] 测试CSL在大批次大小和长序列下的可扩展性
- [ ] 验证CSL训练收敛和稳定性

### 4.2 边缘情况处理
- [ ] 使用单样本批次测试CSL
- [ ] 处理统一文化标签的情况（全部相同/全部不同）
- [ ] 测试CSL在缺失或无效专家输出时的表现
- [ ] 验证CSL在零向量和数值边缘情况下的表现
- [ ] 测试CSL在极端相似度值（0.0、1.0）下的表现

### 4.3 集成测试
- [ ] 使用分布式训练（多GPU）测试CSL
- [ ] 验证CSL与梯度累积和混合精度的兼容性
- [ ] 使用不同优化器配置测试CSL
- [ ] 验证CSL与现有评估流水线的兼容性
- [ ] 使用不同数据集格式和大小测试CSL

## 验证标准

### 功能验证
- CSL损失函数产生三个有意义的损失组件
- CSL与现有联合训练流水线无缝集成
- 保持所有现有配置的向后兼容性
- CSL参数可通过训练脚本配置

### 性能验证
- CSL计算开销 < 总训练时间的10%
- 内存使用增加 < 基线训练的20%
- 使用CSL保持训练收敛稳定性
- 所有CSL组件在训练期间贡献非零梯度

### 质量验证
- 所有边缘情况的全面错误处理
- CSL架构和使用的清晰文档
- 所有相似性计算中的数值稳定性
- 适当的张量管理和梯度流保持